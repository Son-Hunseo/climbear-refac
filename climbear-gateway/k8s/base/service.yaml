apiVersion: v1
kind: Service
metadata:
  name: climbear-gateway-service
  namespace: climbear
spec:
  type: NodePort
  selector:
    app: climbear-gateway
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080